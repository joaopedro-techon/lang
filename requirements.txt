# Espelha as dependencias do pyproject.toml.
# Preferir `pip install -e .`, que tambem cria o comando `springboot-agent`.
langgraph>=0.2.57
langchain-anthropic>=0.3.0
langchain-core>=0.3.0
python-dotenv>=1.0.0
